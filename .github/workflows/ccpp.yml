name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-16.04
    
    steps:
    - uses: actions/checkout@v2
    - name: make
      run: ./makeFirstTime.sh
    - name: Binary
      run: ./makeBin.sh
